# go-kit

Personal collection of small Go libraries and helpers.
APIs may change at any time.

## Supported kits
* `httpkit` - Collection of constans (headers/MIME types) as well as handlers and middlewares that could be used with `net/http`
